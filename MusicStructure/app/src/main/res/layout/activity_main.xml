<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.example.aatifshah.musicstructure.MainActivity">

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="This is where I can see all three (for now) additional activities. There is a button for me to go to my Library, a button for me to Browse, and a button for me to go to my player or now-playing"
        android:textSize="35dp" />


    <Button
        android:id="@+id/playerButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:text="Go to Player" />

    <Button
        android:id="@+id/libraryButton"
        android:layout_width="match_parent"
        android:layout_above="@id/playerButton"
        android:layout_height="wrap_content"
        android:text="Go to Library" />

    <Button
        android:id="@+id/browseButton"
        android:layout_above="@id/libraryButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Go to Browse" />

</RelativeLayout>
